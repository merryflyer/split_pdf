# 经验文件：PDF 按小结（小节）自动拆分

> 目的：把"按标题层级拆分 PDF、且标题可能出现在页面中间"的做法固化成可复用、可改进的规则。
> 关联技能：`pdf-section-splitter`（用户级 skill，见 ~/.workbuddy/skills/pdf-section-splitter）
> 适用对象：带文字层的教材/文档类 PDF（非扫描件）。

---

## 一、核心问题

用户要"按小结/小节拆分"，但很多小节标题并不在页首，而是**出现在页面中间**。
如果按整页切，会把上一节的尾部内容错归到本节，或把本节标题前的半页丢给上一节。

**解法**：识别每个标题在页内的 y 坐标，遇到标题在页面中间时，把那一页**按 y 坐标矢量裁开**——
上半归上一节、下半（从标题起）归本节。这样不丢字、不串内容、文字仍清晰。

---

## 二、必须遵守的规则（核心方法论）

### 规则 1：标题识别用「字号 + 编号」双判据
- 层级约定：`第N章` = 章(1) ；`X.X` = 节(2) ；`X.X.X` / `本章小结` = 小结(3)。
- 正文约 11 号字，顶部页码更小，据此与标题区分。
- **排除目录页**：目录里的"第一章…第二章…"是中文数字 + 小字号，不是正文标题，**必须排除**（否则会被误判成章）。正文章节是阿拉伯数字"第1章" + 大字号。
- 不要纯靠字号分层级：有些书把"1.1 节标题"也排成和章一样大的字号，应改用**编号深度**（X.X=节，X.X.X=小结）判层级更稳。

### 规则 2：标题在页面中间 → 用 `show_pdf_page` 按 y 坐标矢量裁切
- 用 PyMuPDF 的 `page.show_pdf_page(target_rect, src_doc, page_no, clip=Rect(0, y_top, w, y_bottom))`。
- 每节的首页：从本小节标题 y 向下裁（上方归上一节）。
- 每节的末页：裁到下一小节标题 y（下方归下一节）。
- 这是矢量操作，文字不模糊；不要用"渲染成图片再裁"（会失真）。

### 规则 3：跨页父标题必须计入下一节（关键防坑）
**这是最容易出 bug 的地方。**
- 切分点必须是**全局有序的结构标题（章/节/小结）**。
- 每小节文件的起点 = 它前面、且与上一小节之间出现的**第一个结构标题**（可能是上一页的节/章标题）。
- 反例（旧版 bug）：`1.2 程序的开发与运行`（节标题）在 p36，`1.2.1` 在 p37，不在同一页。
  若"上抬父标题"只查同页，则 `1.2.1` 没被抬到 `1.2 节`，仍从 p37 起 → `1.1.1` 的区间一路延伸到 p37，**把整页 p36 吞进 1.1.1**。
- 修正：标题列表全局按 (页, y) 排序后，从某个小节标题向前回溯，遇到的第一个 `lv < 本层` 的结构标题即为其起点（遇同层标题则停，起点取自身）。这样父标题恒归"下一个小节"。

### 规则 4：章 / 节标题与引言要作为上下文带进首个小节
- 章标题 + 章引言（如"第1章"及开篇段）应归入**本章第一个小节**文件（作为上下文），不要丢在缝里。
- 规则 3 的"向前回溯取最近结构父标题"天然实现这一点：第一章首个小结 `1.1.1` 向前回溯到 `第1章`，引言即被找回。

### 规则 5：标题跨行要合并，避免文件名截断
- 有些标题换行成两行（如 `2.1.1 信` / `息的二进制编码`），只抓首行会导致文件名不完整。
- 处理：同页、同层级、纵向相邻（间距 < ~40pt）、且续行不以数字开头 → 合并成完整标题文本（仅用于文件名；定位 y 仍用首行）。

### 规则 6：区间严格相邻，杜绝空文件 / 重叠 / 空隙
- 每节的**终点严格等于下一节的起点**（下一节起点 y 即本节末页的裁切底）。
- 同页内做防重叠钳制即可；跨页靠"终点=下一节起点"天然衔接。
- 校验：生成后必须确认**0 个空文件**、首尾页不串下一节标题。

### 规则 7：文件名净化
- 标题中的 ` < > : / \ | ? * " ` 等替换为下划线，避免文件系统报错。

### 规则 8：OCR 残损只影响文件名，不修文字层
- 源 PDF 文字层可能有 OCR 残损（如 `侬凰754`=IEEE754、`<3`=C、`ι.ι`=1.1），**只影响个别文件名，正文内容不受影响**。
- PDFKit 的 OCR 命令（`ocr_locate`/`edit_scanned`/`extract_text --ocr_fallback`）只对"无文字层的扫描页"生效，**修不了已存在于文字层里的错别字**；那需用 `smart_edit` / `ir_modify` 手动替换。

---

## 三、运行方式

复用脚本 `split_pdf_by_section.py`（项目根目录）或 skill 内 `scripts/split_by_section.py`：

```bash
# 全本按小结(默认 level=3)拆分
python split_pdf_by_section.py

# 只拆第一章做示例(输出到 split_output_ch1/)
python split_pdf_by_section.py --chapter 1

# 换粒度: 按节(level=2, 约23个) / 按章(level=1, 6个)
python split_pdf_by_section.py --level 2

# 只看方案不生成
python split_pdf_by_section.py --preview
```

参数：`--level {1,2,3}`（1=章,2=节,3=小结）、`--chapter N`（单章）、`--preview`。

---

## 四、已知坑 & 待改进

1. **部分节标题 OCR 损坏无法识别**：本书 `1.1`、`1.3` 的节标题被识别成 `ι.ι` 之类乱码、字号还排成章标题大小 → 节粒度(level=2)下 1.1、1.3 会分别并入相邻的 1.2、1.4，拆不干净。**小结粒度(level=3)反而最稳**。→ 改进方向：对"疑似乱码但字号=章"的行做容错，或先 `ocr_locate` 校正。
2. **目录/大纲页干扰**：检测正文起点要跳过前面的目录、前言、大纲页（本书正文从第 33 页起）。→ 改进：以首个"第1章"出现页作为正文起点。
3. **合并续行阈值 40pt 为经验值**，对特殊排版可能误并/漏并。→ 可改为基于行高统计自适应。
4. **`--chapter N` 依赖章标题页码定位**；若章标题被 OCR 破坏则失效。→ 改进：支持手动指定章节页码范围。
5. 本经验与 skill 应**持续迭代**：每遇到新 PDF 的新坑，就回来补充规则 4/八、更新坑点列表，并用 `SkillManage` 改进 skill 脚本。

---

## 五、交付验收清单（每次拆分后必查）

- [ ] 某小节的父标题（如 `1.2 程序的开发与运行`）**只出现在正确子小节**，未被上一节吞掉（文本校验）。
- [ ] **0 个空文件**（页数统计）。
- [ ] 章引言已找回进首个小节（不被丢在缝里）。
- [ ] 末节文件**不含下一章**内容（无跨章泄漏）。
- [ ] 标题在页面中间的小节，其首页确实以本小节标题开头（中页切分生效）。
