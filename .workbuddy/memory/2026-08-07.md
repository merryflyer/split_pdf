# 2026-08-07

## split_pdf 工程分析
- 完整分析了项目的 4 个文件（split_pdf_by_section.py / _ch1_split_pdfkit.py / README.md / 经验_PDF按小结拆分.md）
- 对比两脚本差异：主脚本已修复旧版所有已知坑（OCR容错、跨页父标题、参数化），旧脚本仅作参考
- 发现高危问题：render() 无空文件校验、末节可能跨章泄漏（缺旧版的边界钳制逻辑）
- 中危：跨行合并阈值 40pt 硬编码、字号阈值 11.8 依赖特定书籍
- 已提改进优先级：P0=创建Skill+空文件校验，P1=README+删旧脚本，P2=去重+字号参数化
- 经验文档提到的 pdf-section-splitter skill 实际未创建

## 全量修复 + 提交（commit ee25614 已 push 到 main）
- 模块化重构：拆成 pdfsplit 包（classifier/detector/sections/renderer），split_pdf_by_section.py 变薄 CLI，新增 --min-size 参数
- 修复 4 个 bug：render 空文件防护、章末内容丢失钳制（clamp_last_section_to_next_chapter）、章引言丢失（回溯持续越过多级父标题）、OCR 还原误伤正文（孤立字符才映射，"1.4 IO 模型"/"1.3 L2 cache" 不再误判）
- 分析中深挖出的两个新高危 bug：章引言掉出所有小节（经验文档规则 4 声称已解决实际没有）、"IO/L2" 前缀误改导致节误判成小结
- 新增 37 项 pytest（含 fitz 合成 PDF 端到端验证），全部通过；CLI 冒烟通过
- 删除 _ch1_split_pdfkit.py、补全 README、加 .gitignore、经验文档补充坑 7/8
- 创建用户级 skill：~/.workbuddy/skills/pdf-section-splitter（SKILL.md + scripts/pdfsplit 包 + references/experience.md），已通过官方校验打包，脚本冒烟通过
- venv 环境：/Users/seagull/.workbuddy/binaries/python/envs/default 已装 pymupdf + pytest
